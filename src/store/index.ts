import { createStore } from "vuex";
import mutations from '@/store/mutations'

export default createStore({
  state: {
    imageBlob: []
  },
  getters: {},
  mutations,
  actions: {},
  modules: {},
});
